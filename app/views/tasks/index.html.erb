<% if user_signed_in? %>

<h1>Hello, <%= current_user.email %>!</h1>
<h3>Here are your saved tasks:</h3>

<br>

<%= render(:partial => 'tasks') || 'No entries found.'%>

<br>

<%# <h3>Here are the tasks due for today:</h3>

<br>

<%= render(:partial => 'filter') || 'No entries found.' %> 
<% end %>